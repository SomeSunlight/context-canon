# Reference architecture

This Markdown file is a faithful transcription placeholder for `reference.pdf` in the shared onboarding fixture.

It exists to exercise ContextCanon's rule that opaque office/PDF source material must have a same-basename Markdown transcription before it participates in semantic onboarding.
