name: issue-36-applicator-v2

on:
  push:
    branches:
      - fix/issue-30-fresh-onboarding-root

permissions:
  contents: write

jobs:
  apply:
    if: ${{ !contains(github.event.head_commit.message, '[issue-36-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/issue-30-fresh-onboarding-root
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Materialize Issue 36 patcher
        shell: bash
        run: |
          gzip -dc .github/issue36-helper.gz > .github/agent_issue36_apply_runtime.py
          python - <<'PY'
          from pathlib import Path
          p = Path('.github/agent_issue36_apply_runtime.py')
          text = p.read_text(encoding='utf-8')
          text = text.replace('_orientation, title = lines[0][len(prefix):].rsplit(" — ", 1)', '_orientation, title = lines[0][len(prefix):].split(" — ", 1)')
          p.write_text(text, encoding='utf-8')
          compile(text, str(p), 'exec')
          PY
      - name: Apply Issue 36 patch
        run: python .github/agent_issue36_apply_runtime.py
      - name: Install ContextCanon
        run: python -m pip install -e .
      - name: Focused regressions
        run: python -m unittest tests.test_onboarding_placement_review tests.test_onboarding_placement_audit tests.test_onboarding_reset tests.test_onboarding_workspace_checkpoint -v
      - name: Full deterministic suite
        run: python -m unittest discover -s tests -v
      - name: Verify generated ContextCanon output
        run: contextcanon check --all .
      - name: Verify clean diff
        run: git diff --check
      - name: Finalize PLAN checkpoint
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('PLAN.md')
          text = p.read_text(encoding='utf-8')
          text = text.replace(
              '- [ ] Run focused/full deterministic regressions, zero-drift check and clean-diff verification; keep PR #31 draft/unmerged for continued owner validation.',
              '- [x] Run focused/full deterministic regressions, zero-drift check and clean-diff verification; keep PR #31 draft/unmerged for continued owner validation.'
          )
          text = text.replace(
              'Verification is still pending on the final product tree; PR #31 remains draft and unmerged.',
              'The applicator workflow passed the focused regressions, complete deterministic suite, `contextcanon check --all .`, and `git diff --check` on the final product tree. PR #31 remains draft and unmerged pending continued `Llama_Dispatcher` owner validation.'
          )
          p.write_text(text, encoding='utf-8')
          PY
      - name: Commit clean product tree
        shell: bash
        run: |
          rm -f .github/agent_issue36_apply_runtime.py
          rm -f .github/agent_issue36_apply.py
          rm -f .github/issue36-helper.gz
          rm -f .github/workflows/agent-issue36-apply.yml
          rm -f .github/workflows/agent-issue36-apply-v2.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Separate STEP-08 source edit review sheets (#36) [issue-36-applied]'
          git push origin HEAD:fix/issue-30-fresh-onboarding-root
